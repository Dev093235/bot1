name: Auto Deploy Node.js Bot

on:
  push:
    branches:
      - main  # Jab bhi main branch update hogi, ye action chalega

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📂 Clone Repository
        uses: actions/checkout@v3

      - name: 🔧 Fix Git Extraheader Issue
        run: git config --global --unset http.https://github.com/.extraheader || true

      - name: 🔥 Install Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: 📦 Install Dependencies
        run: npm install  # Agar `package.json` hai to dependencies install karega

      - name: 🖥️ Install Xvfb (for GUI Apps)
        run: sudo apt-get install -y xvfb

      - name: 🚀 Run the Bot
        run: xvfb-run --auto-servernum -- node bot.js
        env:
          PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: "true"
          PUPPETEER_EXECUTABLE_PATH: "/usr/bin/google-chrome-stable"
